/**
 * Page-specific layout for the `matrix` page family (McpCut console). Filled in
 * by its wave; until then intentionally empty so the aggregator order is fixed.
 */
export const CSS_PAGE_MATRIX = `
`
