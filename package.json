{
  "name": "mcp-journal",
  "version": "0.1.0",
  "description": "Transparent stdio proxy for MCP servers with a persistent, secret-redacted JSONL journal",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "mcp-journal": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ulid": "^2.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.5.0",
    "vitest": "^3.2.7"
  }
}
