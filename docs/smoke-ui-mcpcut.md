# Смок фронта «McpCut» (перенос админки на дизайн от 2026-08-22)

**Статус: ПРОЙДЕН** (прогон 2026-08-22, ветка `feat/ui-mcpcut-redesign`, сборка из рабочего дерева после волн 0–3).
План и решения — `.claude/plans/ui-mcpcut-redesign.plan.md`. Скриншоты — `docs/design/mcpcut/`.

**Чем прогоняли:** Chromium (Playwright 1.62, бинарник `chromium-1228`, headless), два прохода — с JS и **с выключенным JS**; живой стенд на подменённом `HOME` (реальный `~/.mcp-journal` не тронут), посев данных только через публичный CLI и фейкового stdio-клиента поверх `connect` (как в смоке M4): 4 сервера (2 stdio / 2 http, env/headers с `vault:`-ссылками), 3 агента (один отозван), 3 админа, каталог `m4-server.mjs` в карантине → выпуск → вызов `read_note` (allow) → вызов `write_note` (висящая заявка, таймаут 900 с) → перерегистрация сервера со схемой v2 → рекарантин `write_note` (`changed`, `surfaceDelta: widened`) + решение `quarantined`.

## Стенд

```
export SMOKE=/tmp/smoke-mcpcut && mkdir -p "$SMOKE"
npm run build
HOME=$SMOKE node dist/cli.js ui --port 8093          # owner-токен печатается на stderr один раз
# посев — см. seed-скрипт в отчёте сессии (CLI: vault init/set, server add ×4, agent create/grant/revoke,
# admin add ×2, connect-сессии с tools/list, tools/call, quarantine approve, server remove/add v2)
```

## Что проверялось и факт

| # | Проверка | Ожидание | Факт |
|---|---|---|---|
| 1 | `GET /` без сессии | 303 → `/login` | ✅ |
| 2 | `/login`: шрифт | `document.fonts.check('16px Silkscreen') === true`, `.brand` рендерится Silkscreen; в Network только same-origin `/assets/silkscreen-400.woff2` | ✅ загружен с same-origin, внешних запросов нет |
| 3 | `/login`: нет `data-events-url`, нет табов/sign-out; консоль пуста | — | ✅ |
| 4 | Вход form-POST из Chromium (класс регрессии M4 `Origin: null`) | 303 → `/`, дашборд | ✅ |
| 5 | Дашборд: 4 тайла (held/quarantined/servers/agents), очередь одобрений с живой заявкой (`m4/write_note`, аргументы с `[REDACTED]`), последние решения, полоса серверов с пометкой `quarantined` | — | ✅ `[REDACTED]` на месте, в журнале решений `REQUIRE-APPROVAL-PENDING`, `QUARANTINED`, `ALLOW` |
| 6 | Табы по роли owner | Dashboard · Journal · Quarantine · Servers · Agents · Vault · Admins | ✅ |
| 7 | `/journal`: список сессий, открытие сессии, раскрытие записи (`<details>`) — payload виден | — | ✅ (первый скриншот «пустого» тела оказался снимком посреди 320-мс анимации `row-in`; с паузой 450 мс — содержимое на месте) |
| 8 | `/quarantine`: карточка `m4/write_note` · `changed` · `surfaceDelta: widened` · дифф `properties.force — property-added`, approve/reject | — | ✅ |
| 9 | `/servers`: карточки, поиск в шапке (клиентский фильтр), `+` у активного таба открывает drawer регистрации; раскрытые карточки: command/args (по одному на строку), env/headers (`vault:`-бейдж vs literal), список тулзов из инвентаря с пометкой карантина и ссылкой «review in quarantine» | — | ✅ |
| 10 | Отклонённая регистрация (секрет-литерал в env) | 400, drawer остаётся открыт, `[role=alert]` с подсказкой про вольт, секрет не возвращается в HTML | ✅ «env.TOKEN: value … looks like a secret literal; secrets must not live …» |
| 11 | `/agents`: матрица прав, `revoked`, drawers create/grant, `+` | — | ✅ |
| 12 | `/admins`: ростер, роли-пилюли, inline-формы role/rotate/remove в ряд, drawer add | — | ✅ |
| 13 | `/vault` (owner): имена и даты, без значений | — | ✅ |
| 14 | Sign out | 303 → `/login` | ✅ |
| 15 | **Без JS**: вход формой, `/servers`, открытие drawer кликом по `<summary>` | страница работоспособна | ✅ |
| 16 | CSP: `securitypolicyviolation` на всех страницах | 0 | ✅ 0 (inline-стилей нет; `font-src 'self'` добавлен) |
| 17 | Консоль/сеть | только ожидаемый 400 из п.10 | ✅ |

## Находки (закрыты в ходе смока)

1. Поиск в шапке рендерился столбиком (`form { flex-direction: column }` перекрывал `.search`) — `flex-direction: row` на `.search`.
2. Inline-формы `/admins` (`form.inline`) по той же причине ставили кнопку под селект — `flex-direction: row` на `form.inline`.
3. `code` с `word-break: break-all` рвал `mcp-journal admin add` посреди слова на `/login` — `overflow-wrap: anywhere`.
4. Пилюли-радио `.choice` наследовали `flex-direction: column` от `label` — поправлено в `forms.ts` (находка агента волны 3).

## Не покрыто

- Firefox/WebKit-плечо (как и в предыдущих смоках); режим `--behind-tls`.
- Живое SSE-обновление очереди в браузере глазами (контракт `data-live-region` не менялся и покрыт `tests/ui/page-contracts.test.ts`); заметка: счётчики «N held» в шапках панелей Dashboard/Quarantine лежат вне live-региона и обновляются только перезагрузкой.
- Экран `Groups` дизайна — не переносился: сущности «группа» нет в бэкенде (M5.5, пункт 2, открытые вопросы владельца PRD).

## Дополнение 2026-08-22 (вечер): анимации экрана Auth

По запросу владельца перенесён весь функционал прототипа `Auth.dc.html`, кроме поля «Admin»: декоративный слой (блоки слева влетают `login-in-left/top/bottom`, заливка полосами, маршрут к «консоли» справа с бегущей точкой, набор строки журнала, стопка вкладок; скролл раскрывает стопку в широкие строки и гасит карточку), Show/Hide токена, чекбокс «Keep this session» (без `name`, не постится; подпись — реальный TTL 8 ч), хореография входа (поля «уезжают в консоль», белая заливка экрана, затем **настоящий** `form.submit()` → `/`). Реализация: отдельный ассет `/assets/login.js` (`src/ui/assets/login-js*.ts`), keyframes/классы в `css/page-login.ts`, геометрия через CSSOM (CSP не нарушается). Без JS — прежняя форма; при `prefers-reduced-motion` слой остаётся скрытым. Playwright-прогон: блоки видимы через 2,5 с, через ~9 с в стопке 2 строки, Show/Hide переключает `type`, скролл растягивает консоль до ширины экрана, вход после анимации приземляется на `/` за ~5,7 с; 0 ошибок консоли, 0 нарушений CSP.

## Дополнение 2026-08-24: дашборд приведён к Dashboard.dc.html

По запросу владельца `/` перестроен точно под макет Dashboard.dc.html: 4 тайла со спарклайнами (детерминированные корзины высот `.tb-h*` — CSP не допускает inline-стилей), сетка 1.9fr/1fr — таблица «Call journal» слева (колонки Time · Server/tool · Lat · Status, пилюли-фильтры по серверам `?server=`, выбор строки `?sel=` подсвечивает её и наполняет карточку «Call detail» справа под очередью одобрений), внизу сетка «Servers» с барами активности (`.svw-*`). Фильтр и выбор — обычные GET-параметры, всё работает без JS; поиск в шапке — клиентский фильтр строк журнала (контракт `data-filter-item`/`data-filter-empty` из app.js). Lat показывает `—`, пока у decision-записей нет `durationMs`; в args-боксе — `rule` и `argsHash` (сами аргументы на дашборд не выносятся). Прогон Chromium: пилюли и выбор строки работают, поиск «write_note» сузил 10 строк до 3, пустое состояние показывается, 0 ошибок консоли/CSP. Скриншоты: `dash-new/dash-sel/dash-filter/dash-search.png` (скретч), контрольный — `docs/design/mcpcut/`. По фидбеку владельца клик по строке больше не перезагружает страницу: `/assets/dashboard.js` переносит выделение и переписывает карточку детали из data-атрибутов строки (только `textContent`; href сессии принимается лишь с префиксом `/journal?`), URL обновляется `history.replaceState` — глубокая ссылка `?sel=` остаётся рабочей и без JS. Проверено Chromium: маркер `window` переживает клик, деталь/URL обновляются, 0 ошибок.

## Дополнение 2026-08-24 (2): /servers приведён к Servers.dc.html + редактирование

`/servers` перестроен под макет: карточки с белой рамкой в двух видах — квадратные тайлы (открытая занимает 2 колонки; `align-items: start` — соседи НЕ растягиваются при раскрытии одной, каждая `<details>` независима) и список (переключатель ▦/≡ в командной панели, `?view=list`); свёрнутый тайл: точка+имя, пилюля транспорта (stdio — сплошная), пилюля «N quarantined» (shimmer), цель и мета-строка («N args · M env · T tools» / «protocol · N headers»). Форма регистрации — модальный оверлей в стиле макета (одна колонка, пилюли транспорта, группа только активного транспорта через `:has()`, args — textarea ПО ОДНОМУ НА СТРОКУ — аргумент с запятой/пробелом больше не ломается, белый callout про вольт): открывается ТОЛЬКО через «+» у таба (видимая кнопка «Register a server» убрана — summary скрыт визуально, но остаётся в DOM для клавиатуры; no-JS путь — `href="/servers?add=1"`). **Новое: редактирование сервера** — кнопка Edit на раскрытой карточке → `?edit=<name>` префиллит форму из реестра (имя read-only: это ключ грантов/карантина; POST `/servers/edit`, owner-only, имя из формы игнорируется — берётся `original`), с тем же confirm-интерстишелом, что и регистрация («править stdio-команду = та же RCE-власть»), аудит `server.update`; в реестре новый `updateServer` (валидация + типизированный not-found, TDD). Найден и закрыт латентный баг: radio `protocol` всегда постился со значением по умолчанию, и КАЖДАЯ stdio-регистрация из браузера падала бы 400 «unrecognized key protocol» — теперь protocol включается в кандидата только для http (покрыто тестами). Прогон Chromium: раскрытие одной карточки не меняет высоту соседних, ▦/≡ работает, «+» открывает модалку без перезагрузки и × закрывает, edit m4 end-to-end (правка env → confirm «Save changes to “m4”?» → сохранение → правка видна на карточке → откат), 0 ошибок консоли/CSP. Скриншоты: `srv-grid/srv-modal2/srv-list3/srv-confirm/srv-after-edit.png` (скретч). Не перенесено из макета: модалка Tools с переключателями on/off и release — у «выключения тулза» нет бэкенда (кандидат в M5.5), release живёт на /quarantine; статус running/degraded — плоскость не мониторит серверы.

### Уточнение 2026-08-24: построчный редактор args

По фидбеку владельца args в модальной форме — построчный редактор, как в макете (`/assets/servers.js`): нумерованный инпут на аргумент (00, 01, …), «×» у строки, «+ Add argument», счётчик «N / 100»; textarea остаётся в форме скрытым источником POST (каждое изменение строк синкается в него), так что запрос байт-в-байт равен no-JS отправке, а без JS форма показывает прежний textarea. Значения ходят только через `input.value`/`textContent` — разметка из них не собирается. Прогон: префилл из реестра, добавление/удаление с перенумерацией, confirm-интерстишел показывает ровно введённые строки, 0 ошибок.
