# ADR-0011: Открытый релиз под Apache-2.0 вместо пилотов с design-партнёрами

- **Статус**: принято (2026-09-03, решение владельца S3 по итогам security-аудита)
- **Область**: дорожная карта (M6 «Пилоты» и трек B), лицензия и файлы у корня
  (`LICENSE`, `NOTICE`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, поле `license` в
  `package.json`), состав публичного дерева, формулировки в README.

## Контекст

PRD строил валидацию гипотезы на 2–3 design-партнёрах, чей аудитор примет экспортированный
отчёт; ROADMAP держал контрольную точку «конец недели 8: нет партнёра → пивот». Трек B за
четыре недели дал: две публичные ветки на GitHub (один ответ — смежный вендор, дизайн-вход в M5),
два поста в LinkedIn без отклика (канал закрыт 19.08), один предметный комментарий в Threads без
квалификации, пять email-касаний 12.08 без ответов; follow-up 19.08 подготовлен, отправка в
pipeline не зафиксирована. Партнёра нет. Код при этом обогнал валидацию: M1–M5 и M5.5 закрыты,
сквозной security-аудит пройден (`docs/security-audit-2026-09.md`), 4533 теста.

Аудит поставил владельцу вопрос S3 — нужен ли независимый проход до первого партнёра. Ответ
владельца снял сам вопрос: писем больше не будет, партнёров не ищем, продукт выходит как открытый
код.

## Решение

1. **Лицензия — Apache License 2.0.** Разрешительная, с явной патентной лицензией; привычна
   enterprise-пользователям, которым продукт адресован. `LICENSE` — канонический текст
   (sha256 `cfc7749b…523d30`), `NOTICE` — имя проекта и правообладатель, `package.json` —
   `"license": "Apache-2.0"`.
2. **M6 «Пилоты» и трек B закрываются без исполнения.** Их место занимает «открытый релиз»:
   версия 0.1.0, `CHANGELOG.md`, `SECURITY.md` (приём уязвимостей через private vulnerability
   reporting GitHub, без почты в репозитории), `CONTRIBUTING.md` (правила: две прод-зависимости,
   TDD, инварианты, ADR, формулировки про журнал).
3. **Публичное дерево не содержит** `docs/pitch/` (имена и адреса реальных людей, черновики
   писем) и `.claude/` (внутренние планы, PRD, отчёты волн). ADR, смоки, аудит и дизайн-скриншоты
   остаются — это документация решений, а не переписка. Поскольку история приватного репозитория
   содержит эти файлы, **публичный репозиторий — это санированная копия с новой историей**, а не
   push приватного `main`; приватный репозиторий с полной историей и планами остаётся рабочим.
4. **Независимого security-прохода до релиза не будет.** Аудит остаётся внутренним и так и назван
   в README, SECURITY.md и самом отчёте; это честнее, чем ждать аудитора, которого не будет.
5. **Формулировки не меняются.** Журнал — «tamper-evident with an external anchor», без
   «tamper-proof» и «audit-ready». Открытый релиз не повод смягчать квалификатор.

## Последствия

- Строка PRD «сегмент — enterprise/compliance, валидация — design-партнёры» больше не
  описывает способ валидации: валидация теперь — использование открытого кода и обратная связь
  через issues. Позиционирование продукта («контроль доступа + доказательный журнал, не
  детекция и не generic gateway») остаётся.
- README перестаёт быть инструментом питча: таблица статуса остаётся источником истины о том,
  что есть, но ссылка на «pitch and landing material» уходит.
- Бэклог, отложенный «до данных пилотов» (индексная таблица агентов, 8-sink профиль, пауза SSE
  по `drain`, инструмент сверки якоря с `prune`), переходит в режим «по запросу пользователей».
- Публикация — отдельный шаг с явным согласием владельца: адрес репозитория, org, ветка.

## Когда пересматриваем

- Появится пользователь, готовый платить за поддержку или хостинг, — тогда отдельное решение о
  коммерческой модели (open-core, поддержка), не правка этого ADR.
- Кто-то из пользователей принесёт аудитора — S3 возвращается как «внешний проход по запросу».
- Появится внешний контрибьютор с потоком PR — тогда решение о рабочем репозитории (перенос работы
  в публичный); до того PR из публичного переносятся в приватный руками (поправка 2026-09-24).

## Поправка 2026-09-23 — решения владельца D1–D4 по итогам исследования перспектив

Источник: `.claude/PRPs/reports/prospects-verdict-2026-09-23.md` (совет из четырёх голосов поверх
обзора рынка на 23.09). Владелец принял все четыре рекомендации.

- **D1 — история публичного репозитория сохраняется** (меняет п. 3). Вместо копии с новой историей —
  `git filter-repo` по приватной истории: удаляются пути `docs/pitch/` и `.claude/`, коммиты и даты
  остаются. Причина: без истории заявление о том, как продукт построен, ничем не подтверждается, а
  один коммит на 88 тыс. строк читается как сгенерированный код без разбора. Цена, которой не было
  у новой истории: чистить приходится **всю** историю, а не только текущее дерево —
  `--replace-text` для абсолютного пути домашнего каталога (5 коммитов) и адреса S2 (1 коммит),
  переписать сообщения 3 коммитов с названиями компаний из аутрича, решить, публикуется ли
  `CLAUDE.md` (сейчас он в git и описывает трек B), имя участника публичного треда в `ROADMAP.md`
  и адрес автора в метаданных коммитов (mailmap на noreply или как есть). Проверка санитайзером
  идёт по истории, а не по HEAD.
- **D2 — язык.** Добавляется англоязычный `docs/ARCHITECTURE.md` на одну страницу, ADR-0007
  (доказательный журнал) переводится на английский. Остальные ADR, аудит и смоки остаются
  русскими; `docs/adr/README.md` говорит об этом прямо.
- **D3 — удалённая консоль (`--remote`, ADR-0014) и мост по сети (`connect --url`, ADR-0015)
  публикуются с пометкой *preview*** и не показываются в верхней части README и в демо. Порог
  снятия пометки — внешний отзыв о безопасности или первый пользователь, который ими пользуется.
- **D4 — заморозка новых функций** до контрольной точки через 30 дней после публикации. Разрешено
  только то, что снижает порог входа (README, Quick start, путь через Docker, `npx` на чистой
  машине) и исправления по issues.

Позиционирование для запуска уточнено (не меняет п. 5 и формулировки о журнале): первым
показывается личный журнал и стоп-кран для разработчика на Claude Code или Cursor, доказуемость
истории — третьим пунктом; enterprise-контроль против бесплатных gateway не заявляется.

## Поправка 2026-09-24 — публикация: репозиторий, выгрузка истории, выпуск 0.1.0

План `.claude/PRPs/plans/completed/agent-pool-npm-publish.plan.md` (фаза 6 PRD пула), отчёт
`.claude/PRPs/reports/agent-pool-npm-publish-report.md`. Поправка D1–D4 выше не повторяется —
эта исполняет её.

| # | Решение | Почему |
|---|---|---|
| **NP1** | Публичный репозиторий — `github.com/RostislavMatov/mcpcut` | учётка, под которой залогинен `gh` |
| **NP2** | Создание репозитория входит в фазу; пакет публикуется с тега этого репозитория | исходник той версии, которой отдают токен агента, должен быть виден |
| **NP3** | Версия **0.1.0** — первый реальный выпуск: `[Unreleased]` слит в `[0.1.0]` | 0.1.0 от 03.09 нигде не публиковалась и не тегировалась |
| **NP4** | 0.1.0 публикует владелец руками; дальше — workflow по тегу: OIDC trusted publishing + staged publishing (одобрение каждой версии с 2FA) | npm не даёт привязать trusted publisher к пакету, которого нет; компрометация CI не должна выпускать версию без человека |
| **R5** | Выгрузка — `tools/release/export-public.mjs`: `git filter-repo` по **свежему клону** ветки; правила — `.claude/release/filter/` (`paths.txt`, `replace-text.txt`, `replace-message.txt`, `forbidden.txt`, `mailmap`), в публичную историю не попадают | `filter-repo` переписывает репозиторий на месте — рабочий репозиторий не трогается никогда |
| **R6** | ПДн исправляются и в HEAD приватного репозитория, и правилами для истории | новые коммиты чисты без фильтра; старые версии файлов остаются в истории |
| **R7** | Автор и коммиттер всей публичной истории — `Rostislav Matov <96149587+RostislavMatov@users.noreply.github.com>` | личная почта не публикуется; трейлеры `Co-Authored-By` остаются как часть заявления о процессе |
| **R8** | Репозиторий сначала приватный: CI на Linux, санитайзер по истории, потом public. После публикации выгрузка отказывает, если публичный `main` (или локальный) не предок нового, — история растёт только fast-forward; `--allow-rewrite` — только пока приватный | первое падение CI не публичное; правило фильтра, задевшее опубликованный коммит, ловится до push |
| **R13** | `CLAUDE.md` исключён из всей публичной истории | рабочая память проекта: трек B, партнёры, внутренняя хроника |
| **R14** | Выгрузка механически проверяет результат при каждом запуске: (а) нет исключённых путей ни в одном коммите; (б) ни один blob не содержит левых частей замен и строк `forbidden.txt`; (в) то же для сообщений; (г) все идентичности — из `mailmap`; (д) повторный фильтр даёт тот же `main`. Находка называет правило, коммит и путь, но не найденную строку | санитайзер — вторая линия; первая должна повторяться механически |

**Что исключено из публичной истории сверх D1** (найдено инвентаризацией и проверками R14):
`.mcpcut-project/` (dogfood-политика: у того, кто запустит `wrap` в клоне, она молча стала бы
проектной политикой, ADR-0005), `CLAUDE.md`, `docs/status-2026-08-12.md` (статус трека B вне
`docs/pitch/`) и восемь скриншотов смоков в `docs/design/mcpcut/` — на одном полный токен
владельца локальной установки, на остальных домашний путь; пиксели текстовым правилом не
исправить, поэтому файлы не входят ни в один публичный коммит (решение владельца 2026-09-24).

**Законное исключение из правила ADR-0013 «старые документы не переписываем»**: то правило — о
переименовании; фильтр переписывает только **публичную копию**, приватная история не меняется
никогда.

**Чего нет**: двусторонней синхронизации (PR из публичного переносятся руками), branch protection,
issue templates, Dependabot, CODEOWNERS (появятся по запросу).

